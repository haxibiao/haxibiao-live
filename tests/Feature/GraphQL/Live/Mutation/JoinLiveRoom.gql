#加入直播间
mutation JoinLiveMutation($live_id: Int!) {
  joinLive(live_id: $id) {
    id
    title
    pull_url
    cover
    count_audience
    user {
      id
      name
      avatar
      is_followed
    }
  }
}
